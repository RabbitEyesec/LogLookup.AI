"""Dashboard static assets (served by engine.api.server).

This package exists so setuptools ships ``static/`` inside the installed
distribution — the web UI is package data, not importable code.
"""
